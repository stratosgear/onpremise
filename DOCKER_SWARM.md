Follow original install instructions:
* Make changes to:
  * config.yml
  *  sentry.conf.py
  * .env w/ environment variables
* Run .install

The provided docker-compose-swarm.yml is configured for execution
on hypervasis2 server

